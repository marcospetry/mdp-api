# Copie para .env e preencha com as credenciais do ambiente.
# Produção/Dokploy: use host/porta internos da stack PostgreSQL.
# Teste local via túnel SSH: normalmente localhost:15432.
DATABASE_URL=postgresql+psycopg://USUARIO:SENHA@localhost:15432/mdp
SECRET_KEY=troque-esta-chave
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
CONTATO_DESTINO=
